@model ArtistProductViewModel

@{
    ViewData["Title"] = "Produkt information";
}

<div class="d-flex justify-content-center m-4">
    <h2>@Model.Products.FirstOrDefault().Title</h2>
</div>

<div class="d-flex justify-content-center m-4">
    <img src="@Model.Products.FirstOrDefault().PictureLink" />
</div>

<div class="d-flex justify-content-center mt-4 mb-3">
    <h3>Lägg till Artist</h3>
</div>
<div class="d-flex justify-content-center m-3">
    <h4>@Model.Products.FirstOrDefault().ShortDescription</h4>
</div>
<div class="d-flex justify-content-center m-3">
    <p>@Model.Products.FirstOrDefault().LongDescription</p>
</div>

<table class="table">
    <tr>
        <td>
            Pris: @Model.Products.FirstOrDefault().Price kr
        </td>
        <td>
            I lager: @Model.Products.FirstOrDefault().Stock st
        </td>
        <td>
            Dimension: @Model.Products.FirstOrDefault().Width x @Model.Products.FirstOrDefault().Height
        </td>
        <td>
            Stil: @Model.Products.FirstOrDefault().Style
        </td>
        <td>
            @Model.Products.FirstOrDefault().CreationDate
        </td>
        <td>
            @if (@Model.Products.FirstOrDefault().IsPaintingOfTheWeek)
            {

            }
        </td>
    </tr>
</table>
<div class="d-flex w-100 m-3">
    <button class="btn-primary">Köp</button>
</div>