@using Microsoft.AspNetCore.Identity
@using ArtWebshop.Data
@inject AppDbContext appContext
@inject ProductDbContext ProductContext
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

@model Artist

@{ ViewData["Title"] = "ArtistPage"; }


<h1>@Model.ArtistName</h1>

<div>
    <!--<div class="d-flex justify-content-start">
        <img src="Model.Artist.FirstOrDefault().ProfilePicture" />
    </div>-->
    <br />
    <div class="col-md-1" style="width:200px">
        @Model.Presentation
    </div>
    <br />
    <br />
    @Html.ActionLink("Ändra", "EditArtist", "Artist", new { id = "ÄndraArtist" }, null)
</div>


