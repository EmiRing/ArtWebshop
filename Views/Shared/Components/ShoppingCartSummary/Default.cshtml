@model ShoppingCartViewModel

@if(Model.shoppingCart.ShoppingCartItems != null)
{
    <li class="nav-item dropdown d-block">
        <a asp-controller="ShoppingCart"
           asp-action="Index" class="dropdown-toggle nav-link" data-bs-toggle="dropdown">
            <i class="fas fa-shopping-cart"></i><b class="caret"></b></a>
        <ul class="dropdown-menu dropdown-menu-end dropend container-xxl" style="width: 400px">
            <li>
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th class="text-left col-6" scope="col">Produkt</th>
                            <th class="text-right col-2" scope="col">Antal</th>
                            <th class="text-right col-3" scope="col">Pris</th>
                            <th class="col-1" scope="col"></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model.shoppingCart.ShoppingCartItems)
                        {
                            <partial name="_cartRow" model="item" />
                        }
                    </tbody>
                    <tfoot>
                        <tr>
                            <td colspan="2" class="text-right">Total:</td>
                            <td colspan="2" class="text-left">
                                @Model.ShoppingCartTotal.ToString("c")
                            </td>
                        </tr>
                    </tfoot>
                </table>
            </li>
        </ul>
    </li>
}



@*<table>
    <thead>
        <tr>
            <th>Antal</th>
            <th>Tavla</th>
            <th class="text-right">Pris</th>
            <th class="text-right">Subtotal</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var row in Model.shoppingCart.ShoppingCartItems)
        {
            <partial name="_cartRow" model="row" />
        }
    </tbody>
    <tfoot>
        <tr>
            <td colspan="3" class="text-right">Total:</td>
            <td class="text-right">
                @Model.ShoppingCartTotal.ToString("c")
            </td>
        </tr>
    </tfoot>
</table>*@