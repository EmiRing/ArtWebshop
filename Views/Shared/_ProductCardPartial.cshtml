@model ArtistProductViewModel

@foreach (var item in Model.Product)
{
    <div class="col-3 card pt-2">
        <div class="image-thumb">
            <img src="@item.PictureLink" class="frame" width="305" height="305" />
        </div>
        <table class="table-sm">
            <tr>
                <td scope="col">Verk:</td>
                <td scope="col">@item.Title</td>
            </tr>
            <tr>
                <td scope="col">Konstnär:</td>
                <td scope="col">@item.Artist.ArtistName</td>
            </tr>
            <tr>
                <td scope="col">Pris:</td>
                <td scope="col">@item.Price kr</td>
                <td scope="col"><button class="btn-primary" onClick="">Köp</button></td>
            </tr>
        </table>
    </div>
}
<a asp-controller="ShoppingCart" asp-action="Index">Go to cart</a>
<div class="product-options">
    <a id="myWish" href="javascript:;" class="btn btn-mini">Köp </a>
</div>
<div class="alert alert-success" id="success-alert">
    <strong>Success! </strong> 1 Tavla tillagd i kassan.
</div>
